library(Benchmarking)
install.packages("Benchmarking")
?kmeans
load("C:/Users/Ainize/Google Drive/GLOBALL/NEW TRIAL/.RData")
View(teamsper)
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ Team.Territory,
sum(Revenue_[which(datatable$Locality=="Local" |datatable$Locality=="Regional"),]),
data=datatable))
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ Team.Territory,
sum(Revenue_Net[which(datatable$Locality=="Local" |datatable$Locality=="Regional"),]),
data=datatable))
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ Team.Territory,
sum(datatable$Revenue_Net[which(datatable$Locality=="Local" |datatable$Locality=="Regional"),]),
data=datatable))
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ Team.Territory,
sum(datatable$Revenue_Net[which(datatable$Locality=="Local" |datatable$Locality=="Regional")]),
data=datatable))
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ Team.Territory,
function(x) sum(datatable$Revenue_Net[which(datatable$Locality=="Local" |datatable$Locality=="Regional")]),
data=datatable))
View(teamsper)
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ Team.Territory,
function(x) sum(x[which(datatable$Locality=="Local" |datatable$Locality=="Regional")]),
data=datatable))
View(teamsper)
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ c(Locality,Team.Territory,sum,
data=datatable)))
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ c(Locality,Team.Territory),sum,
data=datatable)))
teamsper$local<-as.data.frame(aggregate(Revenue_Net ~ c(Locality,Team.Territory),sum,
data=datatable))
teamsper$local<-as.data.frame(aggregate(Revenue_Net,by=Team.Territory,data=datatable))
teamsper$local<-as.data.frame(aggregate(datatable,by=Team.Territory))
teamsper$local<-as.data.frame(aggregate(datatable,by=Team.Territory,sum))
teamsper$local<-as.data.frame(aggregate(datatable,by=datatable$Team.Territory,sum))
teamsper$local<-as.data.frame(aggregate(datatable,by=levels(datatable$Team.Territory,sum))
teamsper$local<-as.data.frame(aggregate(datatable,by=levels(datatable$Team.Territory),sum))
for (i in names(teamsper)){if datatable$Station.Territory==i{}}
teamsper$local<-as.data.frame(aggregate(datatable,by=levels(datatable$Team.Territory),sum))
levels(datatable$Team.Territory)
levels(datatable$Team.Territory)[1]
levels(datatable$Team.Territory)[1,]
datatable<-as.data.frame(read.xlsx("Datatable.xlsx",1))
4+4
log(9)
setwd("~/ainize_git/blogdown_source")
